"use client";
import React from "react";

export default function TitleThree({title}) {
  return (
    <h2 className="font-sora font-medium text-[22px] md:text-[32px] text-[white] capitalize">{title}</h2>
  );
}
